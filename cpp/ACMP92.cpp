#include <iostream>

using namespace std;

int main() {
    int s, thirdPart;
    cin >> s;
    thirdPart = s / 3;

    cout << (thirdPart / 2) << " " << (thirdPart * 2) << " " << (thirdPart / 2) << endl;

    return 0;
}
